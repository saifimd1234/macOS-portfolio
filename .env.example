# Copy this file to .env (or .env.local) and paste your OpenAI key.
# This file is read ONLY by the serverless function in /api — the key
# is never bundled into the browser. NEVER commit your real key.
OPENAI_API_KEY=sk-your-key-here
